spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/hareram
    username: root
    password: mysql
    initialization-mode: always
security:
  oauth2:
    resource:
      filter-order: 3
  signing-key: <signing-key>
